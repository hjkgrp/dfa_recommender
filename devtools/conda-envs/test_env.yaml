name: test
channels:
  - defaults
  - psi4
  - conda-forge
dependencies:
    # Base depends
  - python
  - psi4
  - pip
  - pytorch

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

