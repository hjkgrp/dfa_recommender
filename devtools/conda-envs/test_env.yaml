name: dfa_rec
channels:
  - defaults
  - psi4
  - conda-forge
dependencies:
    # Base depends
  - python=3.8
  - psi4
  - pip
  - pytorch=1.10.0
  - pandas
  - scikit-learn

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - e3nn
  #  - codecov

