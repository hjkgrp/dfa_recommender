name: dfa_rec
channels:
  - defaults
  - psi4
  - conda-forge
dependencies:
    # Base depends
  - python=3.8.13
  - psi4=1.6.1+5b9f6e3
  - pip
  - pytorch=1.10.2
  - pandas=1.4.3
  - scikit-learn=1.1.1

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - e3nn
  #  - codecov

