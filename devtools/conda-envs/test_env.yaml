name: test
channels:
  - defaults
  - psi4
  - conda-forge
dependencies:
    # Base depends
  - python=3.7.12
  - psi4=1.3.2+ecbda83
  - pip
  - pytorch=1.10.0

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

