name: dfa_rec
channels:
  - defaults
  - psi4
  - conda-forge
dependencies:
    # Base depends
  - python=3.7
  - psi4=1.3.2+ecbda83
  - pip
  - pytorch=1.10.0
  - pandas
  - scikit-learn

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

