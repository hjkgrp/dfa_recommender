name: test
channels:

  - conda-forge
  - psi4
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - pytorch
  - psi4
  - shap
  - numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

