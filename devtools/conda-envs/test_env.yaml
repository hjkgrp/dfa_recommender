name: dfa_rec
channels:
  - defaults
  - psi4
  - conda-forge
dependencies:
    # Base depends
  - python=3.7
  - psi4
  - pip
  - pytorch
  - pandas
  - scikit-learn

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

